export class ValidPriceDictionaries {
    constructor(
        public id: number,
        public startPrice: number,
        public endPrice: number,
        public description: string,
        public name: string
    ) { }
}